{
    "type": "object",
    "examples": [
        {
            "identifier": "sdf6sd",
            "status": "выполняется"
        }
    ],
    "required": [
        "identifier",
        "status"
    ],
    "additionalProperties": false,
    "properties": {
        "identifier": {
            "type": "string",
            "pattern": "^[a-z0-9]{2,5}$",
            "examples": [
                "sdf6sd"
            ]
        },
        "status": {
            "type": "string",
            "enum": ["обрабатывается", "выполняется", "доставлено"],
            "examples": [
                "выполняется"
            ]
        }
    }
}